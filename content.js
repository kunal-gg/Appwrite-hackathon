(() => {
    let youtubePlayer, youtubeLeftControls

    chrome.runtime.onMessage(addEventListener(obj, sender, response => {
        const { type, value, videoId } = obj

        if (type == "NEW"){
            
        }
    }))

})

()